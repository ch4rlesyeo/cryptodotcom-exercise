import CurrencySearch from '@/components/currency-search';
import { ScreenRootView } from '@/components/ui/screen-root-view';

const CurrencySearchScreen: FC = () => {
  return (
    <ScreenRootView>
      <CurrencySearch />
    </ScreenRootView>
  );
};

export default CurrencySearchScreen;
